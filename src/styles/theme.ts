type Colors = {
    black: string;
    lightBlack: string;
    white: string;
}

const colors: Colors = {
    black: '#000000',
    lightBlack: '#797979',
    white: '#FFFFFF'
}

const spacing = {
    1: "0.25rem",
    2: "0.5rem",
    3: "1rem",
    4: "2rem",
    5: "4rem",
    6: "8rem"
};

const theme = {
    colors,
    spacing
}

export default theme